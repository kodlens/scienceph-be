export interface ResourceType {
  id?: number;
  name: string;
  slug?: string;
  active: boolean;
}


export interface FilterType {
  id?: number;
  name: string;
  slug?: string;
  active: boolean;
}
