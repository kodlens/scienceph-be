export default interface ResourceType {
  id?: number;
  name: string;
  slug?: string;
  active: boolean;
}


export default interface FilterType {
  id?: number;
  name: string;
  slug?: string;
  active: boolean;
}