export interface Category {
  id?: number;
  //name?: string;
  category: string;
  slug?: string;
  // description?: string;
  //order_no?: number;
  active?: number;
  // Add other subject properties as needed
}
